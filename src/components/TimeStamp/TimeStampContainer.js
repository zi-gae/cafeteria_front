import React from "react";
import TimeStampPresenter from "./TimeStampPresenter";

const TimeStampContainer = props => {
  return <TimeStampPresenter {...props}></TimeStampPresenter>;
};

export default TimeStampContainer;
